#include <stdio.h>
#include <math.h>
/**
 * main - main block
 * description: maths operation
 * Return: always (0) success
 */
int main(void)
{
    double A = sqrt(9);
    double B = pow(2, 4);
    int C = round(3.14);
    int D = ceil(3.14);
    int E = floor(3.75);
    double F = fabs(-100);
    double G = log(3);
    double H = sin(45); 
    double I = cos(45);
    double J = tan(45);

    printf("%lf\n", B);

    return (0);
}